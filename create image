import cv2
import numpy as np

b = np.zeros((500,900,3))


font = cv2.FONT_HERSHEY_SIMPLEX
img= cv2.putText(b, 'Opencv', (90,300), font, 7,(0,255,255),10, cv2.LINE_AA)

cv2.imshow('image', img)

cv2.waitKey(0)
cv2.destroyAllWindows()
